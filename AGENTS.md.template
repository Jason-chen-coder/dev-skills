# AGENTS.md — Team Always-On Rules

> 这是一份模板文件。使用 Codex / OpenCode 时,把本文件复制到项目根目录并重命名为 `AGENTS.md`,再按 `<!-- team:fill -->` 标记填团队真实情况。
>
> 这份文件会常驻 agent 上下文,所以只放必须永远生效的短规则。更完整的团队治理说明可参考 dev-skills 仓库的 `docs/team-policy.md`,或复制 / 改写到消费项目自己的 `docs/team-policy.md` / wiki。

---

## 1. 行为基线

以下四条原则自包含生效;安装包内的 `references/dev-baseline.md` 是详细版本:

- **不假设**:先查现有上下文;关键业务、契约或授权缺口才问;低风险可逆选择沿用项目惯例并说明重要假设。
- **最小代码**:没要求的不做;抽象要有当前收益;流程长度按风险决定。
- **外科手术式改动**:只动任务需要的代码;不顺手重构、格式化或改邻近注释。
- **可验证成功标准**:完成前给出新鲜验证证据;测试必须断言行为,不是只断言 shape。

这些原则背后的失败模式见 `docs/why-dev-baseline.md`。

---

## 2. 不可破的硬规则

- **不要在没有显式授权的情况下** `git push --force` / `git push --force-with-lease` 到 protected 分支。
- **不要新增运行时依赖**(npm / pip / pub / cargo 等)未经讨论;确实需要时在 PR 描述里说明为什么不能用现有代码或标准库。
- **不要写裸 secret / API key / token / 个人路径** 到任何文件;`.env.example` 只能写占位符。
- **不要修改与本任务无关的代码** —— 即使它看起来值得整理。
- **不要用 LLM 生成的代码替换已有、有 git blame 的代码而不说明**。

---

## 3. Agent 工作方式

- **先读后写**:改文件前先读邻近代码、直接 caller / callee、相关测试和项目配置。
- **冲突不平均**:同一代码库有两种错误处理、状态管理、命名或测试模式时,选一种并解释原因;不要两个都混用。
- **确定性逻辑写成代码**:重试、路由、限流、算术、时间、排序、去重等不要靠 prompt 判断。
- **失败必须可见**:partial failure、skipped rows、truncated output、retry exhaustion 都要进入结果说明。
- **长任务设 checkpoint**:多步 refactor / migration / 批量修改要拆小步,每步可验证、可回退。
- **卡住要换策略**:同一错误或同一 8KB 输入反复循环时,停止猜测,改用 reproduction、instrumentation 或更小假设。

---

## 4. 执行协议

- **小任务直接做**:单命令、typo、窄文档改动或明确单文件小修可以直接执行,但仍要在完成前给出检查结果。
- **大任务先 explore / plan**:跨文件、重构、数据迁移、权限/支付/安全、公开 API、需求不清时,先读现状并给出计划;用户只要求分析时不要写代码。
- **SDD artifact 对齐**:如果存在 `.claude/artifacts/designs|plans|fixes/<slug>.md`,后续实现、验证和 review 要把它当作 source artifact;发现 drift 先说明或更新,不要静默扩范围。
- **写作和 review 分开**:authoring pass 负责创建或修改;review / verification pass 必须重新看 diff、跑命令、拿证据,不能只复述刚才的实现判断。
- **完成前清零 pending**:结束前确认没有未处理 finding、TODO、失败测试或挂起命令;如果仍有风险,明确列出而不是说 ready。
- **失败按归属回流**:验证失败回实现或 `dev-fix`;review P0/P1 先修再 commit;spec / plan blocked 回用户决策,不要硬推进。
- **授权连续有效**:用户已批准的必要步骤继续执行,不在 skill 切换时重复确认;说明重要进展。只要分析、方案或 review 时保持对应模式。

---

## 5. Multi-Agent 协议

- 多 agent 只用于边界清晰的并行探索、实现、验证或 review;详细规则见 `docs/multi-agent-policy.md`。
- 主 agent 始终负责用户沟通、任务拆分、最终整合、git 操作和完成声明。
- 子 agent 必须有明确 ownership,不得修改未分配文件,不得回滚他人改动。
- 子 agent 任务应写明 source artifact、objective、write scope、do-not-edit 和 verification expected。
- verifier / reviewer 应尽量独立于实现上下文,并输出命令证据、changed files 和残余风险。

---

## 6. dev-skills 路由

- 不知道下一步时跑 `dev-auto`;它只指路,不调起其他 skill。
- UI / landing page / 产品界面类工作,首次进入项目前可跑 `dev-design-context`,把设计上下文沉淀到 `.design-context.md`。
- 新功能 / 改功能需求不清时走 `dev-grill-docs`,默认产物为 `.claude/artifacts/designs/<slug>.md`;项目已有文档位置则沿用,明确小改动不强制补 spec。
- `dev-spec` 仅作兼容入口,等价于 `dev-grill-docs --spec-only`。
- 复杂新功能在 `dev-grill-docs` 后可接 `dev-plan --deliberate`。
- feature / refactor / direct hotfix 写生产代码前默认走 `dev-tdd`。
- bug 报告走 `dev-fix`;它负责复现、根因和回归证据,不要再追加一轮 `dev-tdd`。需要反证时用隔离副本,不得 stash 或撤销用户改动。
- 声称完成 / fixed / ready 前必须跑 `dev-verify` 或给出等价 fresh verification evidence。
- commit 请求默认跑 `dev-code-review`;只要 message 时用 `dev-commit-writer`,写 message 不等于 review 通过或授权提交。
- 验证和 review 通过后用 `dev-finish` 处理 PR / merge / keep / discard。

---

## 7. Team Specifics

<!-- team:fill — 保持短。详细规则放项目自己的 docs/team-policy.md 或 wiki。 -->

- Stack:`<填技术栈>`
- Test:`<填测试命令>`
- Lint / format:`<填命令>`
- Default branch:`main`(或 `master`)
- Branch naming:`codex/<short-slug>` for AI work;团队分支规则为 `<填>`
- Commit style:跟随 `git log --oneline -20`;无明显风格时用 conventional commits,subject ≤ 72 chars
- Error handling:`<throw / Result / mixed,选一种>`
- Logger:`<团队 logger>`;禁止调试输出进入主分支
- Do-not-touch:`<列出 migrations / generated files / vendor dirs 等>`

---

## 8. 冲突处理

- 遵守运行环境的指令优先级;本模板和 skill 的默认流程不能覆盖用户明确目标、限制及已有授权。
- 对已明确的用户选择直接执行并在必要时说明取舍;只有缺少关键决定或对应动作授权时才询问,不为同一例外重复确认。
- 如果 skill 输出与本文件冲突,以本文件为准,并把冲突作为 dev-skills bug 反馈。
