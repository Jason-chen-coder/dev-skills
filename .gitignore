# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp
*~

# Local Claude state (skills consume artifacts at the consuming project's path,
# not this repo's. But guard against accidental commits from local testing.)
.claude/artifacts/
.claude/settings.local.json

# oh-my-claudecode local state (session / memory / cooldown caches)
.omc/

# Brainstorming visual companion local drafts
.superpowers/

# Node (in case skills repo ever gains tooling)
node_modules/
*.log

# Swagger skill local credentials, cache, and default exports
/swagger.config.json
/swagger-spec-cache.json
/swagger-api.md
/swagger-api.json
